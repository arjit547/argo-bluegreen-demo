apiVersion: v1
kind: Service

metadata:
  name: argo-demo-preview
  namespace: demo

spec:

  type: ClusterIP

  selector:
    app: argo-demo

  ports:

  - port: 80

    targetPort: 3000

    protocol: TCP