apiVersion: argoproj.io/v1alpha1
kind: Rollout

metadata:
  name: argo-demo
  namespace: demo

spec:

  replicas: 1

  selector:
    matchLabels:
      app: argo-demo

  template:

    metadata:
      labels:
        app: argo-demo

    spec:

      containers:

      - name: argo-demo

        image: 638111422814.dkr.ecr.us-east-1.amazonaws.com/argo:366408334d29d5fcc5488bb9d66cffe2f2afdb3c

        imagePullPolicy: Always

        ports:

        - containerPort: 3000

  strategy:

    blueGreen:

      activeService: argo-demo-active

      previewService: argo-demo-preview

      autoPromotionEnabled: true

      previewReplicaCount: 1
